
{% from "./navLinks.njk" import navLinks %}
{% macro footer(phoneNumber)%}
<hr/>

<div class="bg-off container mx-auto  flex flex-col  lg:flex-row pt-4 lg:pt-8 pb-12 ">

    <div class=" lg:w-1/3">
        {% include "../components/logo.njk"%}
    </div>
    <div class="w-2/3 flex flex-col lg:flex-row mt-4 lg:mt-0">
        <div class="lg:w-1/4">
        <span  style="min-width: 140px" class="text-primary pb-4 text-base font-semibold flex items-center pl-4">          	
            <a value="{{phoneNumber}}" href="tel:"+{{phoneNumber}}>{{phoneNumber}}</a>
        </span>
        {{ navLinks (
            [
                {
                    ref: "/developer",
                    text: "Developers"
                },
                {
                    ref: "/contact",
                    text: "Contact"
                }
            ]
            ,null, "column", "true")
        }}
    </div>
    <div class="lg:w-1/4">
              {{ navLinks (
            [
                {
                    ref: "/about",
                    text: "About"
                },
                {
                    ref: "/blog",
                    text: "Blog"
                },
                {
                    ref: "/",
                    text: "Home"
                }
            ]
            ,null, "column", "true")
        }}
    </div>
    </div>

</div>
{% endmacro %}
